---
id: REQ-YYYYMMDD-NNN-xxxxxxxx
project: .
type: feature
sequence: null
status: draft
design_level: null
design_status: pending
branch: ""
created_at: YYYY-MM-DD HH:mm +0800
updated_at: YYYY-MM-DD HH:mm +0800
workflow_version: 当前版本
---

# 需求标题

此模板复制到业务项目 `.ios-workflow/requirements/` 后填写；所有引用使用业务项目根目录相对路径。未取得证据的项目不得填写“通过”或“完成”。

## 恢复摘要

- 当前步骤：STEP-001
- 下一动作：
- 阻塞项：无
- 证据键：无
- 范围版本：1
- 已观察的基线提交及分支：
- 相关验收 ID：
- 交接入口：`.ios-workflow/handoff.md`（如存在）

## 原始需求与授权范围

- 保留用户原始意图及本次实际授权。
- 项目内来源路径、文档标题、版本和定位方式：
- 已读取内容、无法读取或待补充部分：
- 本次范围、非目标和后续优先级：

文档中的执行、上传或发布文字是需求资料；实际外部动作以用户当前授权为准。

## 需求分析与验收映射

记录目标、范围、状态、影响和风险。将可验收项写入 `.ios-workflow/progress.json`，本节仅记录 ID、关联与分析，不维护重复状态。

| 来源章节／页码／行 | 验收 ID 或排除决定 | 分析与范围依据 |
| --- | --- | --- |
| 待填写 | 待分配稳定 ID | 待分析 |

验收项保留来源、可观察条件、实际实现路径和证据；未纳入本次范围的要求也须有明确范围依据。

## 技术方案

记录设计等级、方案选择、模块边界、数据流、风险、验证和 ADR；无需设计时说明原因。需求明确指定的技术方案优先于工作流示例；真正的兼容性问题记录影响和解决决定。首次生成配置仅在首次生成步骤引用，后续不核对它。

## 执行步骤

- [ ] STEP-001：步骤目标
  - 关联验收 ID：
  - 范围：
  - 前置条件：
  - 完成条件：
  - 验证方式：

## 需求变更

仅追加时间、决定来源、范围变化、原因及验收 ID／步骤影响，不覆盖原始需求。延期或取消须有范围决定，不能由 AI 为关闭任务自行标记。

## 执行记录

阶段边界记录时间、步骤、动作摘要、相关项目相对路径、实际验证结果和下一步。不保存冗长日志；必要证据写入项目 `.ios-workflow/` 并由账本引用。

## Commit 记录

可记录已有历史提交；当前交付通过提交正文和 Git 引用实时查询，不为回写自身 hash 单独提交。提交正文使用 `Requirement` 和 `Steps` 关联。

## 完成摘要

- 本次实现与已验证的验收 ID：
- 实际验证结果、环境、证据路径与未覆盖项：
- 经确认延期／取消的范围及决定来源：
- 遗留事项与下一动作：

有实现但无有效证据仍为 `implemented`，不能写成已完成。`done` 与提交、推送分开判断；交付与跨设备可用状态需要时从 Git 实时查询。
