# Changelog

## Unreleased

- 修复本地化标识可越过生成目录的问题；全部输出先校验路径，并拒绝覆盖已有文件及符号链接重定向。

## 6.2.0 - 2026-09-10

- 规范唯一 Skill 的元数据、Markdown 路由与渐进加载；增加分发包链接和脱离仓库的脚本导入验证，内部参考文档不注册为独立 Skill。
- 追踪规则拆为创建、恢复、验收和同步模块，保留完整事实边界并移除重复通用说明；修正架构说明的过时配置与记录目录。
- Entitlements 变更自动纳入发布检查，保持检查选择器与提交规则一致。
- 范围校验先确认全账本的需求关联有效，避免缺失关联的失败验收项被筛选漏掉。
- 增加只读、分页且限制输出长度的当前任务恢复摘要，保留登记状态与已验证状态的区别，并显式报告缺项与省略内容。
- 增加只读证据采集与复用比较，统一实际输入哈希和 iOS 测试环境指纹；缺项、失败或变化不再被视为可复用成功证据。
- 首次配置诊断一次汇总缺项、未知字段、类型和组合冲突，保持显式完整输入并在写入前拒绝非法配置。
- 增加按变更范围选择检查的内部模块，追溯只核对当前需求与验收项；全项目健康检查须显式选择。
- 补齐进度契约，拒绝 verified 与失败结果矛盾，要求验收条件、需求关联及真实环境和时间；历史缺项只报待补证。
- 区分阶段保存和最终验收，允许如实同步未完成进度；测试 TODO 与 UI 矩阵按实际范围检查。
- 区分首次建项和已有项目需求迭代，后者跳过生成配置流程，避免冲突路由。

## 6.1.0 - 2026-09-10

- 增加通用 iOS 需求文档导入与冲突处理规则，由客户端提取明确选型和项目决定，生成首次配置再创建工程。
- 所有业务运行记录迁为项目内 `.ios-workflow/`，生成器显式允许预先存在的需求记录但拒绝覆盖业务代码或写入 Skill。
- 增强验收项来源、实现和证据追踪及跨设备 Git 交接，缺失或过期证据不得推断为已完成。
- 新增只读进度校验与跨路径恢复测试；初始生成配置仍不参与后续版本核对。

## 6.0.0 - 2026-09-10

- 移除默认配置与自动补全机制，配置示例和字段说明移出 Skill，分发时直接放在 ZIP 最外层，供团队复制修改。
- 配置只用于首次生成工程，输入必须完整；生成器不再输出持久配置快照，后续版本按实际工程和当前需求检查，不读取或核对初始配置。
- 增加方向配置和 NimbleFive 首次生成输入示例，补充分发位置、配置缺失拒绝和不输出快照的验证。

## 5.1.0 - 2026-09-10

- 增加 iOS App 发布与分发规范，覆盖 Release 预检、Archive 校验、签名、导出、TestFlight、App Store 审核、发布监控和更高 Build 热修复。
- 增加发布计划、发布报告和专项 Checklist，并将版本、签名、商店元数据及发布自动化变更接入条件门禁。
- 明确上传、处理、测试、审核和发布是独立状态；外部状态变更必须由用户当前要求明确授权，敏感凭据不得进入仓库或日志。

## 5.0.0 - 2026-09-10

- 增加团队分发包生成器、接入说明和版本 Tag 发布工作流；发布包只包含 Skill 与接入文件，不包含项目运行记录。
- 增加分发包内容、排除项、版本格式和 SHA-256 校验测试。
- 将运行记录目录从 `iOSFlowRecords/` 迁移为全小写隐藏目录 `.ios-workflow/`，并明确它是工作流自定义状态目录，不属于 Codex 官方 Skill 结构。
- 新增配置驱动的多语言文案规则；生成代码中的用户可见文案必须覆盖全部已配置语言，缺少译文时停止生成。
- 项目骨架从 `localization_strings` 生成各语言资源，并增加译文覆盖和实际内容测试。
- 新增暗黑模式支持与手动外观切换配置；暗黑模式关闭时固定浅色，手动切换开启时生成持久化外观策略。
- 增加非法外观配置、Swift/UIKit、SwiftUI 和 Objective-C/UIKit 生成结果测试。

## 4.0.3 - 2026-09-03

- 增加 `ios-workflow` 触发评测语料，覆盖正负样本、显式调用、跨平台边界以及中文、英文和混合语言提示。
- 增加触发语料结构、覆盖范围、样本平衡和描述边界自动化测试。

## 4.0.2 - 2026-09-03

- 明确 Git 提交标题、提交正文和 Checklist 内容默认使用中文。

## 4.0.1 - 2026-09-03

- 将 `ios-workflow` 的触发描述、界面短描述和默认提示改为中文。

## 4.0.0 - 2026-09-03

- 将完整工作流迁移为仓库级 `.agents/skills/ios-workflow` Skill，并增加官方要求的 `SKILL.md` 元数据。
- 根 `AGENTS.md` 只保留始终生效的项目约束和 Skill 触发入口。
- 规范与检查表迁入 `references/`，配置与模板迁入 `assets/`，项目生成器迁入 `scripts/`。
- README、架构文档与自动化测试更新为新的 Skill 路径。

## 3.7.0 - 2026-09-03

- 低风险明确任务使用上下文需求卡和 inline brief，完整需求与技术设计规范改为条件加载。
- 新项目生成时由生成器直接消费配置，避免默认把九份规则和配置全部载入模型上下文。
- 活动索引包含完整恢复摘要，测试证据仅由受测输入失效，纯文档和记录变化不触发重跑。
- Git 交付状态改为从仓库和远端实时查询，禁止为回写当前提交 hash、推送或 tag 状态制造额外提交。

## 3.6.0 - 2026-09-03

- 限定需求持久化条件，低风险单轮任务不再因开始执行而创建运行档案。
- 将活动索引压缩为单个摘要和最后使用 ID，恢复时优先读取 `恢复摘要` 与首个未完成步骤。
- 简单新增页面或内部模块允许使用 brief 设计，并将需求完成状态与 Git 提交、推送状态解耦。

## 3.5.1 - 2026-09-03

- 示例仓库开始跟踪 `iOSFlowRecords`，使需求、项目顺序和测试证据可随 Git 提交共享。
- 删除与仓库实现不一致的 `REQ-20260903-005` 需求及关联测试记录，并保留已使用序号不复用。

## 3.5.0 - 2026-09-03

- 增加真实 `WorkflowDemo`，覆盖项目生成、UIKit/MVVM 计数功能、设置、运行时语言切换、关于我们、单元测试和 UI 测试。
- 修复 UIKit 新项目 Scene Manifest 缺少 SceneDelegate 配置导致启动后白屏的问题。

## 3.4.0 - 2026-09-03

- 增加测试与质量保障模块，统一验收映射、测试分层、最小设备矩阵、证据复用和结果报告。
- 增加环境阻塞、失败分类和 flaky test 有限复跑规则，以及测试计划、报告模板和条件门禁。

## 3.3.0 - 2026-09-03

- 增加内部 iOS 项目生成与配置校验模块，支持 Swift/UIKit、Swift/SwiftUI 和 Objective-C/UIKit。
- 从配置生成架构、导航、本地化策略、中文注释、DesignTokens、测试、隐私清单和 XcodeGen 工程设置。
- 增加项目生成专项规范、提交门禁和自动化测试，并验证三种支持组合可完成模拟器编译。

## 3.2.0 - 2026-09-03

- 增加 brief/full 技术设计分级、Feature/Bug Fix/ADR 模板和编码前设计门禁。

## 3.1.0 - 2026-09-03

- 增加需求分析与任务定义规范，以及按需加载的 Feature 和 Bug 需求模板。
- 增加可见的 `iOSFlowRecords/` 需求台账，支持跨会话恢复、执行步骤、Git 关联和需求追溯门禁。
- 增加项目级需求执行台账，以固定顺序记录所有开始执行过的需求及其状态和提交。

## 3.0.0 - 2026-09-03

- 支持将完整工作流放入任意工作目录的 `iOS_Workflow/` 子目录，并由同级轻量 `AGENTS.md` 统一引入；`Workspace` 不再作为固定目录名。
- 同一任务内对规则文件去重，并避免重复加载已经进入上下文的完整入口。
- 依赖状态和 diff 未变化时复用安装、构建、测试及静态检查证据。
- 提交后立即推送时只执行增量检查，避免重复 Checklist 和 Git 正文输出。
- 将 Swift 与 Objective-C 细则从核心规范拆出并按语言加载，同时压缩运行时规则的重复表述。

## 2.0.3 - 2026-09-02

- 将运行时规则改为按任务路由，避免每次任务加载全部规范。
- 将代码规范拆分为核心、代码生成和三方依赖三个模块。
- 将提交门禁拆分为通用、订阅和数据打点模块；后两者仅在 diff 影响相关功能时读取。
- 将各规范的历史记录集中到本文件，缩短运行时上下文。
- 依赖安装、解析和编译成功时使用摘要输出，失败时只保留相关日志。

## Code standards

- 1.5：要求新项目创建、三方库版本变化和编译前执行对应依赖操作；直接依赖必须精确锁定版本。
- 1.4：明确生成代码注释边界和 pod、SPM、Carthage、none 的依赖方式。
- 1.3：增加提交、推送和 review 的自动 Checklist 门禁。
- 1.2：移除不再需要的 CLI 使用方式。
- 1.1：补充命名和英文大小写约定。
- 1.0：建立 Swift、Objective-C、架构、日志、隐私和测试基线。

## UI standards

- 1.3：增加海外 app 常用语言、标准语言标识和 RTL 专项检查要求。
- 1.2：配置文件升级为带字段注释的 JSONC 格式。
- 1.1：统一 app、text styles、系统组件、代码标识符和计量单位的英文格式。
- 1.0：建立布局、排版、颜色、组件、状态、动效和可访问性基线。
