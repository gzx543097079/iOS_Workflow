# iOS 项目仓库约定

- 用户当前要求和业务项目自身的明确约定优先。
- 保持项目已有语言、UI、架构和模块边界。
- 修改后运行最小相关验证；工作流 Skill、生成器、路由或测试规则变化时运行 `python3 -m unittest discover -s tests -v`。
- 成功命令只报告目标、状态和必要摘要；失败只保留相关日志。
- 修改工作流规则时同步更新 `CHANGELOG.md`。
- iOS 需求、设计、开发、项目生成、测试、提交、推送或 review 使用 `$ios-workflow`。
