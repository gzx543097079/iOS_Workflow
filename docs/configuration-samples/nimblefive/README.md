# NimbleFive 配置验证示例

输入为用户提供的 `NimbleFive-MVP-PRD.md` v1.0（2026-09-10）。未复制原 PRD，也不依赖用户 Downloads 路径运行测试。

`project.json` 是项目复制配置示例后按 PRD 修改并保存的完整项目实例。它可直接作为生成器的 `instance_path`，但本次不生成或开发实际 NimbleFive App。

| 需求 | 实例及处理 |
| --- | --- |
| Swift、SwiftUI、iOS 16 | `language`、`ui`、`deployment_target` |
| iPhone、竖屏、英语 | `target_devices`、`supported_orientations`、`supported_localizations` |
| 指定 Bundle ID、1.0.0 (1) | 标识和版本字段 |
| 流程测试 | 客户端开启 UI 测试 target；不是已完成业务测试 |
| 无后端、JSON 存档、纯 Swift 游戏规则 | `constraints`，待业务设计实现 |
| 广告、主题、10,000 种子验收 | `constraints`，基础生成器不自动实现 |
| MVVM、CocoaPods、系统深浅色 | 示例项目显式选择，来源明确，不冒充 PRD 决定 |

自动化测试验证实例加载、隔离、示例变化不影响项目实例、非法输入写入前拒绝、方向和语言等配置映射。测试生成的临时骨架仅验证生成器输出，结束后清理；不作为可交付业务 App。原 PRD 的业务验收尚未执行。


配置只用于首次生成。后续业务迭代以实际工程和需求为准，不再读取或核对本文件。

## 从文档到项目的演示映射

下表是工作流测试示例，不是 NimbleFive 开发进度。当前未实施这些业务需求，不能标记为已验证。

| 文档来源 | 配置或验收处理 | 当前示例结论 |
| --- | --- | --- |
| §1.1 Swift、SwiftUI、iOS 16、iPhone、竖屏、英语 | 写入首次生成配置的技术与平台字段 | 配置已有映射，不能据此宣称功能完成 |
| §2 P0/P1/明确不做 | 拆分本期验收、延期和非目标 | 不引入后端、账号或订阅 |
| §4 首次引导、首页、结果、练习、主题、设置 | 每条可观察行为分配验收 ID | 待实现及验证 |
| §5 三种小游戏、10,000 种子 | 规则实现与生成器专项验证 | 骨架编译不等于玩法验收 |
| §6–§7 计分、奖励与连续天数 | 注入可控时间，验证幂等及日期边界 | 待实现及验证 |
| §8–§9 广告与隐私 | 记录 SDK/同意/频控/失败路径要求 | 精确 SDK 版本待技术方案决定 |
| §10 存档与迁移 | JSON 存储及损坏恢复验收 | 待实现及验证 |
| §11–§12 反馈与无障碍 | 普通页面动态字号、棋盘特例、Reduce Motion | 通用 UI 建议按业务要求适配 |
| §14–§15 测试与验收 | 将每项与真实证据关联 | 尚无业务验收证据 |
| §16–§19 发布与指标 | 发布材料、渠道和观测任务 | 文档内容不授权实际上传或发布 |

文档内“离线可玩与广告提示边界”“Memory Grid 免费重开扣分口径”“跨版本旧挑战恢复”需要在正式开发时澄清。工作流明确记录这些问题，不能自行填成产品决定。其他类型的 iOS 文档采用同样的来源、约束、配置和验收拆分方式，不要求这些章节名称。

正式执行时，以上映射、原始需求和进度均写入业务项目自身 `.ios-workflow/`；工作流仓库只保留本教学示例和测试夹具。
