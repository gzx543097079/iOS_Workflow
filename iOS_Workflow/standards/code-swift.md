# Swift 规范

- 默认使用 `struct`；需要身份、继承、共享可变状态或 Objective-C 互操作时使用 `class`，无继承需求的 class 标记 `final`。
- 优先 `let`；避免强制解包、强制转换和隐式解包。框架保证安全时让理由清晰可见。
- 使用 `guard` 提前退出；异步逻辑遵循项目现有方案，优先 async/await。
- UI 状态在主线程或 `@MainActor` 更新，并正确处理任务取消和生命周期。
- 访问级别从最小范围开始；按职责和可测试边界拆分类型与方法。
